import Container from "@/components/Container"

const Home = () => {
  return (
    <Container className="flex-1 flex bg-accent mb-4 rounded-[32px] border border-border text-muted-foreground __bg-hero">
      Home
    </Container>
  )
}

export default Home